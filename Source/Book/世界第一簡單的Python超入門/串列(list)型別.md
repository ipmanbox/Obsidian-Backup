［資料A,資料B,資料C,......］

例如：
［57, 'banana', 'apple'］


在串列中加入新元素：
語法：append()

例如：
Agrounp = ['kazu', 'gorou']
Agrounp.append('dai')
Agrounp
['kazu', 'gorou', 'dai']


在串列中刪除元素：
語法：remove()

改變串列中元素的順序：
語法：sort()

文字順序，會依據文字開頭的英文字母順序，依序排列。

數值順序，會依據數值大小，由小至大重新排序。


*若是串列中，同時有文字和數值，會錯誤！

第2-21～2-25頁

2023-12-18 19:59